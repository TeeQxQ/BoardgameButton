//Game variables
int muuttuja = 10;

void gameLogic()
{
  //TODO
  Serial.println("Moikka");
  Serial.println(blink_enabled);
  Serial.println(led_states[0]);
  Serial.println(getNewMessage(0));

  /*
   millis();
   aikaa kulut > raja

  if (state == vaihe)
  {
    if (getNeWMessage(RED) == msg_btn_pressed_short)
    {
      state = uusi_tila;
    }
  }
  else (state = uusi_tila)
  {
    jotain muuta
  }
   */
  
}
